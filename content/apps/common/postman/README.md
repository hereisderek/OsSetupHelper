# postman

API platform.

- **macOS**: Homebrew Cask `postman`.
- **Windows**: winget `Postman.Postman`.
- **Linux**: not wired up. Postman distributes a direct tarball download from postman.com — no distro repo carries it. Left blank deliberately rather than half-implemented.

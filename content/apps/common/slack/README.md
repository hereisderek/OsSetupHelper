# slack

Team chat.

- **macOS**: Homebrew Cask `slack`.
- **Windows**: winget `SlackTechnologies.Slack`.
- **Linux**: not wired up. Slack distributes direct .deb/.rpm downloads from slack.com — no distro default repo carries it and there is no official apt repo. Left blank deliberately rather than half-implemented.

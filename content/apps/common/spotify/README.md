# spotify

Music streaming.

- **macOS**: Homebrew Cask `spotify`.
- **Windows**: winget `Spotify.Spotify`.
- **Linux**: not wired up. `spotify-client` exists only in Spotify's own apt repo (deb.spotify.com), not in any distro's default repos. Left blank deliberately rather than half-implemented — enable later via a `setup_repo_*.yml` hook in this role.

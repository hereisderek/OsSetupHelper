# claude

[Claude](https://claude.com/download) desktop app (Anthropic). Distinct from the `claudcode` CLI role under `cli/common/`.

macOS (Homebrew Cask) and Windows (winget). Linux is unofficial/beta upstream (custom apt repo, not a standard package-manager install) — not wired up here.

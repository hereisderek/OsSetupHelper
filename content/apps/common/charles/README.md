# charles

Charles Web Debugging Proxy. macOS (Homebrew Cask) and Windows (winget). Linux requires adding Charles's own apt/yum repository first, which this role doesn't set up — install manually if needed.

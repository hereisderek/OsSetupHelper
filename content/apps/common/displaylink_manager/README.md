# displaylink_manager

DisplayLink drivers/manager for USB graphics docks. macOS (Homebrew Cask) and Windows (winget). Linux requires adding Synaptics's own apt repository first, which this role doesn't set up — install manually if needed.

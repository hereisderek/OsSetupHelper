# orbstack

OrbStack — Fast, light, simple container and Linux machine runtime for macOS.

- **macOS**: Homebrew Cask `orbstack` (https://orbstack.dev/).
- **Windows / Linux**: Not supported (macOS only).

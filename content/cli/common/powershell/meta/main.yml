---
galaxy_info:
  author: OsSetupHelper
  description: Install PowerShell 7 (Homebrew formula on macOS, winget on Windows; Linux needs Microsoft's own repo, not wired up here).
  license: MIT
  min_ansible_version: "2.10"
  platforms:
    - name: MacOSX
    - name: Windows
    - name: Ubuntu
  galaxy_tags:
    - powershell
    - shell
